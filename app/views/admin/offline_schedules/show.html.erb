<% content_for :title, @offline_schedule.respond_to?(:name) ? @offline_schedule.name : "Offline schedule" %>

<div class="container-lg">
  <div class="mb-6">
    <div class="flex items-center space-x-4">
      <%= link_to admin_offline_schedules_path, class: "btn-ghost text-sm py-1 px-3" do %>
        <%= lucide_icon "arrow-left", class: "w-4 h-4 mr-2" %>
        Back to Offline schedules
      <% end %>
    </div>

    <div class="mt-4 flex items-center justify-between">
      <h1 class="text-3xl font-bold text-gray-900 dark:text-gray-50"><%= @offline_schedule.respond_to?(:name) ? @offline_schedule.name : "Offline schedule ##{@offline_schedule.id}" %></h1>

      <div class="flex items-center space-x-3">
        <%= link_to edit_admin_offline_schedule_path(@offline_schedule.id), class: "btn-primary" do %>
          <%= lucide_icon "pencil", class: "w-5 h-5 mr-2" %>
          Edit
        <% end %>

        <%= link_to admin_offline_schedule_path(@offline_schedule.id),
            method: :delete,
            data: { confirm: "Are you sure you want to delete this offline_schedule?" },
            class: "btn-danger" do %>
          <%= lucide_icon "trash-2", class: "w-5 h-5 mr-2" %>
          Delete
        <% end %>
      </div>
    </div>
  </div>

  <!-- Offline schedule Content -->
  <div class="card bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-700">
    <div class="card-body">
      <div class="space-y-6">
        <!-- Schedule date -->
        <div>
          <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">预约日期</label>
          <div class="text-lg font-medium text-gray-900 dark:text-gray-100"><%= @offline_schedule.schedule_date&.strftime('%Y年%m月%d日') %></div>
        </div>

        <!-- Schedule time -->
        <div>
          <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">预约时间段</label>
          <div class="text-lg font-medium text-gray-900 dark:text-gray-100"><%= @offline_schedule.schedule_time %></div>
        </div>

        <!-- Location -->
        <div>
          <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">预约地点</label>
          <div class="text-lg font-medium text-gray-900 dark:text-gray-100"><%= @offline_schedule.location %></div>
        </div>

        <!-- Max attendees -->
        <div>
          <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">最大参加人数</label>
          <div class="text-lg font-medium text-gray-900 dark:text-gray-100"><%= @offline_schedule.max_attendees %></div>
        </div>

        <!-- Current attendees -->
        <div>
          <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">当前预约人数</label>
          <div class="text-lg font-medium text-gray-900 dark:text-gray-100"><%= @offline_schedule.current_attendees %></div>
        </div>

        <!-- Status -->
        <div>
          <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">状态</label>
          <div class="text-lg font-medium text-gray-900 dark:text-gray-100">
            <% if @offline_schedule.status == 'available' %>
              <span class="badge badge-success">可预约</span>
            <% elsif @offline_schedule.status == 'full' %>
              <span class="badge badge-gray">已满员</span>
            <% else %>
              <span class="badge badge-warning">已取消</span>
            <% end %>
          </div>
        </div>

        <!-- Metadata -->
        <div class="pt-6 border-t border-gray-200 dark:border-gray-600">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
              <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">ID</label>
              <div class="text-sm text-gray-900 dark:text-gray-100 font-mono"><%= @offline_schedule.id %></div>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Created</label>
              <div class="text-sm text-gray-600 dark:text-gray-400">
                <%= @offline_schedule.created_at.strftime("%B %d, %Y at %I:%M %p") %>
                <span class="text-gray-400">(<%= time_ago_in_words(@offline_schedule.created_at) %> ago)</span>
              </div>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Last Updated</label>
              <div class="text-sm text-gray-600 dark:text-gray-400">
                <%= @offline_schedule.updated_at.strftime("%B %d, %Y at %I:%M %p") %>
                <span class="text-gray-400">(<%= time_ago_in_words(@offline_schedule.updated_at) %> ago)</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
