<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
<link rel="manifest" href="/manifest.json">
<meta name="theme-color" content="#221d39">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="turbo-cache-control" content="no-cache">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="renderer" content="webkit">
    <meta http-equiv="cleartype" content="on">
    <meta name="HandheldFriendly" content="True">
    <meta name="MobileOptimized" content="320">
    <% if @disable_error_tracking %>
      <meta name="disable-error-tracking" content="true">
    <% end %>
    <title>
    <%- if content_for?(:title) %>
      <%= yield(:title) + ' | ' + Rails.application.config.x.appname %>
    <%- else %>
      <%= Rails.application.config.x.appname %>
    <%- end %>
    </title>
    
    <!-- Open Graph Meta Tags -->
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= content_for?(:title) ? yield(:title) + ' | ' + Rails.application.config.x.appname : Rails.application.config.x.appname %>">
    <meta property="og:site_name" content="<%= Rails.application.config.x.appname %>">
    <meta property="og:description" content="<%= @page_description || '立即开启你的人生蜕变 - 在青狮营 2.0，带你开启 AI 时代的人生蜕变。' %>">
    <meta property="og:image" content="<%= request.base_url + asset_path('og-image.png') %>">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:url" content="<%= request.original_url %>">
    
    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= content_for?(:title) ? yield(:title) + ' | ' + Rails.application.config.x.appname : Rails.application.config.x.appname %>">
    <meta name="twitter:description" content="<%= @page_description || '立即开启你的人生蜕变 - 在青狮营 2.0，带你开启 AI 时代的人生蜕变。' %>">
    <meta name="twitter:image" content="<%= request.base_url + asset_path('og-image.png') %>">
    
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= render 'layouts/qiniu_sdk' %>
    <%= content_for?(:head) ? yield(:head) : '' %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <!-- Load error handler early to catch all errors -->
    <%= javascript_include_tag 'error_handler' if Rails.env.development? %>
    <%= javascript_include_tag 'application', type: "module" %>
    <%= favicon_link_tag asset_path("app-favicon.png") unless Rails.env.test? %>
  </head>
  <body class="flex flex-col min-h-screen text-foreground <%= body_class %>">
    <main class="flex-1 flex flex-col bg-surface">
      <%= render 'shared/navbar' unless @full_render %>
      <%= render 'shared/flash_messages' %>
      <%= yield %>
    </main>
    <% if Rails.env.development? %>
      <div id="system-monitor-stream"
           data-controller="system-monitor"
           data-system-monitor-signed-name-value="<%= Turbo::StreamsChannel.signed_stream_name("system_monitor") %>"
           data-turbo-permanent>
      </div>
    <% end %><!-- stimulus-validator: allow-script -->
<script>
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', () => {
      navigator.serviceWorker.register('/service-worker.js')
        .then(registration => console.log('ServiceWorker registered:', registration.scope))
        .catch(error => console.error('ServiceWorker registration failed:', error));
    });
  }
</script>

  </body>
</html>
