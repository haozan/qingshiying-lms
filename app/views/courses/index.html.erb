<!-- 课程首页 -->
<%= render 'shared/promotional_banner' %>
<div class="min-h-screen bg-surface">
  <!-- 继续学习卡片 -->
  <% if @last_position %>
    <section data-testid="home-first-section" class="pt-28 pb-12 bg-gradient-to-br from-primary/10 to-secondary/10">
      <div class="container-xl mx-auto px-6">
        <div class="card-elevated max-w-4xl mx-auto p-8 rounded-2xl hover:shadow-xl transition-shadow cursor-pointer" onclick="window.location='<%= lesson_path(@last_position[:lesson]) %>'">
          <div class="flex items-center gap-4 mb-4">
            <%= lucide_icon "play-circle", class: "w-8 h-8 text-primary" %>
            <div>
              <h2 class="text-2xl font-bold text-foreground">继续学习</h2>
              <p class="text-muted">从上次学习的地方开始</p>
            </div>
          </div>
          
          <div class="flex items-center gap-4">
            <div class="flex-1">
              <p class="text-lg font-semibold text-foreground mb-1"><%= @last_position[:course].name %></p>
              <p class="text-secondary">
                第 <%= @last_position[:chapter].position + 1 %> 章 · 
                第 <%= @last_position[:lesson].position + 1 %> 节 · 
                <%= @last_position[:lesson].name %>
              </p>
            </div>
            
            <%= lucide_icon "arrow-right", class: "w-6 h-6 text-primary" %>
          </div>
        </div>
      </div>
    </section>
  <% end %>

  <!-- 课程列表 -->
  <section class="<%= @last_position ? 'py-16' : 'pt-28 pb-16' %>">
    <div class="container-xl mx-auto px-6">
      <div class="text-center mb-12">
        <h1 class="text-4xl font-bold text-foreground mb-4">全部课程</h1>
        <p class="text-lg text-secondary">选择适合你的学习路径</p>
      </div>

      <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
        <% @courses.each do |course| %>
          <div class="card-elevated group hover:shadow-2xl transition-all duration-300 overflow-hidden rounded-2xl">
            <!-- 课程头部渐变 -->
            <div class="h-32 bg-gradient-to-br <%= course_gradient_class(course) %> relative overflow-hidden">
              <div class="absolute inset-0 opacity-20">
                <div class="absolute top-4 right-4 w-24 h-24 bg-surface rounded-full blur-2xl"></div>
              </div>
              
              <div class="relative z-10 p-6 flex items-center justify-between">
                <div class="w-12 h-12 bg-surface/20 backdrop-blur rounded-xl flex items-center justify-center">
                  <%= lucide_icon course_icon(course), class: "w-6 h-6 text-surface" %>
                </div>
                
                <div class="flex flex-col gap-1">
                  <% if course.free? %>
                    <span class="badge-success text-xs">免费课程</span>
                  <% else %>
                    <span class="badge-warning text-xs">整年更新</span>
                    <span class="badge-success text-xs">支持线下</span>
                  <% end %>
                </div>
              </div>
            </div>

            <!-- 课程内容 -->
            <div class="p-6">
              <h3 class="text-2xl font-bold text-foreground mb-3 group-hover:text-primary transition-colors">
                <%= course.name %>
              </h3>
              
              <p class="text-secondary mb-4 line-clamp-2">
                <%= course.description || "系统学习#{course.name},掌握核心技能" %>
              </p>

              <!-- 课程工具 -->
              <% if course_tool(course).present? %>
                <% tool = course_tool(course) %>
                <div class="flex items-center gap-2 mb-4 text-sm">
                  <span class="font-semibold text-foreground">工具：</span>
                  <span class="font-bold text-foreground"><%= tool[:name] %></span>
                  <% if tool[:self_developed] %>
                    <span class="badge badge-warning text-xs">自研</span>
                  <% end %>
                </div>
              <% end %>

              <div class="flex items-center gap-4 mb-6 text-sm text-muted">
                <div class="flex items-center gap-2">
                  <%= lucide_icon "book-open", class: "w-4 h-4" %>
                  <span><%= course.chapters.count %> 章</span>
                </div>
                <div class="flex items-center gap-2">
                  <%= lucide_icon "video", class: "w-4 h-4" %>
                  <span><%= course.lessons.count %> 节</span>
                </div>
              </div>

              <!-- 价格信息 -->
              <% unless course.free? %>
                <div class="mb-6 p-4 bg-surface-elevated rounded-lg border border-border">
                  <div class="space-y-2">
                    <% if course.original_price.present? && course.original_price > 0 %>
                      <div class="flex items-center justify-between">
                        <span class="text-sm text-muted">原价</span>
                        <span class="text-sm text-muted line-through">¥<%= sprintf('%.0f', course.original_price) %></span>
                      </div>
                    <% end %>
                    
                    <% if course.current_price.present? && course.current_price > 0 %>
                      <div class="flex items-center justify-between">
                        <span class="text-sm font-medium text-foreground">现价</span>
                        <span class="text-lg font-semibold text-primary">¥<%= sprintf('%.0f', course.current_price) %></span>
                      </div>
                    <% end %>
                    
                    <% if course.early_bird_price.present? && course.early_bird_price > 0 %>
                      <div class="flex items-center justify-between">
                        <span class="text-sm font-medium text-orange-600 dark:text-orange-400 flex items-center gap-1">
                          <%= lucide_icon "zap", class: "w-3 h-3" %>
                          早鸟价
                        </span>
                        <span class="text-xl font-bold text-orange-600 dark:text-orange-400">¥<%= sprintf('%.0f', course.early_bird_price) %></span>
                      </div>
                    <% end %>
                    
                    <% if course.original_price.blank? && course.current_price.blank? && course.early_bird_price.blank? %>
                      <div class="flex items-center justify-between">
                        <span class="text-sm font-medium text-foreground">年价</span>
                        <span class="text-lg font-semibold text-primary">¥<%= sprintf('%.0f', course.annual_price) %></span>
                      </div>
                    <% end %>
                  </div>
                </div>
              <% end %>

              <!-- 课程权益 -->
              <% unless course.free? %>
                <div class="text-sm text-muted space-y-2 mb-4">
                  <div class="flex items-center gap-2">
                    <%= lucide_icon "infinity", class: "w-4 h-4" %>
                    <span>课程内容永久查阅</span>
                  </div>
                  <div class="flex items-center gap-2">
                    <%= lucide_icon "calendar", class: "w-4 h-4" %>
                    <span>一年内可预约线下面授</span>
                  </div>
                </div>
              <% end %>

              <!-- 订阅状态 -->
              <% if @subscriptions[course.id]&.active? %>
                <%= link_to course_path(course), class: "btn-primary w-full flex items-center justify-center gap-2" do %>
                  <%= lucide_icon "play-circle", class: "w-5 h-5" %>
                  <span>继续学习</span>
                <% end %>
              <% else %>
                <div class="space-y-2">
                  <% if current_user %>
                    <!-- 已登录：直接购买 -->
                    <%= button_to purchase_course_path(course), method: :post, class: "btn-primary w-full flex items-center justify-center gap-2", data: { turbo: false } do %>
                      <%= lucide_icon "shopping-cart", class: "w-5 h-5" %>
                      <span>立即购买</span>
                    <% end %>
                  <% else %>
                    <!-- 未登录：跳转到登录页 -->
                    <%= link_to sign_in_path, class: "btn-primary w-full flex items-center justify-center gap-2" do %>
                      <%= lucide_icon "log-in", class: "w-5 h-5" %>
                      <span>登录后购买</span>
                    <% end %>
                  <% end %>
                  <%= link_to course_path(course), class: "btn-outline w-full flex items-center justify-center gap-2" do %>
                    <%= lucide_icon "eye", class: "w-5 h-5" %>
                    <span>查看详情</span>
                  <% end %>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </section>

  <!-- Bundle Offer Section -->
  <section class="py-16 bg-surface-elevated">
    <div class="container-xl mx-auto px-6">
      <div class="max-w-2xl mx-auto">
        <!-- Card: Bundle Offer -->
        <div class="card bg-gradient-to-br from-primary/15 to-secondary/15 border-2 border-primary">
          <div class="card-body">
            <div class="flex items-center gap-3 mb-4">
              <%= lucide_icon "gift", class: "w-7 h-7 text-primary" %>
              <h3 class="text-xl font-bold text-primary">3 课联报 特惠</h3>
            </div>
            
            <!-- 价格信息 -->
            <div class="space-y-3">
              <div class="flex items-center justify-between">
                <span class="text-sm text-muted">原价</span>
                <span class="text-base text-muted line-through">￥15000 元</span>
              </div>
              
              <div class="flex items-center justify-between">
                <span class="text-sm font-medium text-foreground">现价</span>
                <span class="text-xl font-semibold text-primary">￥12000 元</span>
              </div>
              
              <div class="flex items-center justify-between">
                <span class="text-sm font-medium text-orange-600 dark:text-orange-400 flex items-center gap-1">
                  <%= lucide_icon "zap", class: "w-3 h-3" %>
                  早鸟价
                </span>
                <div class="flex items-center gap-2">
                  <span class="text-2xl font-bold text-orange-600 dark:text-orange-400">￥9000 元</span>
                  <span class="badge badge-sm bg-red-500 text-white border-0">
                    立减￥3000
                  </span>
                </div>
              </div>
              
              <div class="mt-2 pt-3 border-t border-border">
                <p class="text-sm font-medium text-base-content/80">支持公户付款</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Contact Prompt -->
        <div class="mt-6 text-center">
          <p class="text-base-content/70 text-sm">
            <span class="inline-flex items-center gap-2">
              <%= lucide_icon "arrow-down", class: "w-4 h-4" %>
              <span>3 课联报下方扫码联系创始人镐赞</span>
              <%= lucide_icon "arrow-down", class: "w-4 h-4" %>
            </span>
          </p>
          <!-- QR Code -->
          <div class="mt-6 flex justify-center">
            <div class="w-48 h-48 bg-base-content/5 border-2 border-border rounded-lg flex items-center justify-center shadow-lg">
              <%= image_tag "qr-code.png", class: "w-full h-full object-contain p-3", alt: "联系创始人镐赞" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>
