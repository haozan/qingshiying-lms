<div class="container-lg py-12">
    <div class="card-elevated">
      <div class="card-body">
        <div class="flex items-center justify-between mb-8">
          <h1 class="text-3xl font-bold text-primary">当前会话详情</h1>
          <span class="badge-success flex items-center">
            <span class="w-2 h-2 bg-success rounded-full mr-2"></span>
            活跃
          </span>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
          <!-- User Information -->
          <div class="card-flat">
            <div class="card-body">
              <h3 class="text-lg font-medium text-primary mb-4 flex items-center">
                <svg class="w-5 h-5 text-muted mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                </svg>
                用户信息
              </h3>
              <dl class="space-y-3">
                <div>
                  <dt class="form-label">姓名</dt>
                  <dd class="text-primary font-medium"><%= @user.name %></dd>
                </div>
                <div>
                  <dt class="form-label">邮箱</dt>
                  <dd class="text-primary font-medium"><%= @user.email %></dd>
                </div>
                <div>
                  <dt class="form-label">注册时间</dt>
                  <dd class="text-secondary"><%= @user.created_at.strftime("%Y年%m月%d日") %></dd>
                </div>
              </dl>
            </div>
          </div>

          <!-- Session Information -->
          <div class="card-flat">
            <div class="card-body">
              <h3 class="text-lg font-medium text-primary mb-4 flex items-center">
                <svg class="w-5 h-5 text-muted mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                </svg>
                会话信息
              </h3>
              <dl class="space-y-3">
                <div>
                  <dt class="form-label">会话 ID</dt>
                  <dd class="text-primary font-mono text-sm"><%= @session.id %></dd>
                </div>
                <div>
                  <dt class="form-label">开始时间</dt>
                  <dd class="text-primary">
                    <%= @session.created_at.strftime("%Y年%m月%d日 %H:%M") %>
                    <span class="text-muted text-sm">(<%= time_ago_in_words(@session.created_at) %>前)</span>
                  </dd>
                </div>
                <div>
                  <dt class="form-label">最后活跃</dt>
                  <dd class="text-primary">
                    <%= @session.updated_at.strftime("%Y年%m月%d日 %H:%M") %>
                    <span class="text-muted text-sm">(<%= time_ago_in_words(@session.updated_at) %>前)</span>
                  </dd>
                </div>
              </dl>
            </div>
          </div>
        </div>

        <!-- Device & Security Information -->
        <div class="card-flat mb-6">
          <div class="card-body">
            <h3 class="text-lg font-medium text-primary mb-4 flex items-center">
              <svg class="w-5 h-5 text-muted mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
              </svg>
              设备与安全
            </h3>
            <dl class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <dt class="form-label">IP 地址</dt>
                <dd class="text-primary font-mono"><%= @session.ip_address || '未知' %></dd>
              </div>
              <div>
                <dt class="form-label">用户代理</dt>
                <dd class="text-primary break-words text-sm">
                  <%= @session.user_agent.present? ? @session.user_agent : '未知设备' %>
                </dd>
              </div>
            </dl>
          </div>
        </div>

        <!-- Security Notice -->
        <div class="alert-warning mb-6">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg class="h-5 w-5 text-warning" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
              </svg>
            </div>
            <div class="ml-3">
              <h3 class="text-sm font-medium">安全提示</h3>
              <p class="mt-1 text-sm">
                如果您不认识此会话或认为您的账户已被盗用，请立即登出并修改密码。
              </p>
            </div>
          </div>
        </div>

        <!-- Action Buttons -->
        <div class="card-footer">
          <div class="flex flex-col sm:flex-row gap-3">
            <%= link_to "查看所有会话", devices_session_path, class: "btn-ghost" %>
            <%= link_to "编辑资料", profile_path, class: "btn-primary" %>
            <%= button_to "登出", sign_out_path, method: :delete,
                class: "btn-danger",
                data: { confirm: "确定要登出吗？" } %>
          </div>
        </div>
    </div>
  </div>
</div>
