require 'rails_helper'

RSpec.describe CourseBundle, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:course_bundle)
  #   expect(record).to be_persisted
  # end
end
